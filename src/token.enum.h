#ifndef TP_COMPILO_TOKEN_ENUM_H
#define TP_COMPILO_TOKEN_ENUM_H

enum Token {
    PLUS,
    MULT,
    OPEN,
    CLOSE,
    EXPR,
    EOL,
    VAL,
    EMPTY
};

#endif //TP_COMPILO_TOKEN_ENUM_H
